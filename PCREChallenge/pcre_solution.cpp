#include "pcre_solution.h"

/* TODO: identify vendor source(s) to include from for backwards compatibility,
interoperability, etc, and with better instructions how to include it without
compiler errors complaining about namespaces and such. */
#include <pcre.h>

namespace pcre_ns {

    bool is_match(const std::string& l, const std::string& x) {
        //TODO: implement me with PCRE in view
        return false;
    }
}
